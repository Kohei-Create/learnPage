document.addEventListener('DOMContentLoaded',() => {
    const logContainer = document.getElementById('card-inner');

    if (!logContainer) return;

    try {
        const response = await fetch('./assets/data/index.json')
        const {  }
    }
});